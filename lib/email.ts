export async function sendRSVPEmail({
  to,
  memberName,
  eventName,
  eventDate,
  rsvpUrl,
}: {
  to: string;
  memberName: string;
  eventName: string;
  eventDate: string;
  rsvpUrl: string;
}) {
  // Resend logic here
}